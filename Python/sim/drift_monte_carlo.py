"""Drift Monte Carlo stub — Phase E2 placeholder."""

from __future__ import annotations


def run_drift_monte_carlo(*, samples: int = 100) -> dict[str, float]:
    """Return placeholder drift statistics until E2 sweep ships."""
    return {"samples": float(samples), "mean_drift_ppb": 10.0, "stub": 1.0}
